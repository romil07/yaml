resources:
  packages:
   - package: somePackage
     type: Nuget
     connection: gh-rg
     name: OctocatApp
     
stages:
 - stage: ConsumeArtifact
   jobs:
     - job: job1
       pool: 
        name: Default
       steps:
        - getPackage: somePackage
