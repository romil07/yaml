resources:
  packages:
   - package: somePackage
     type: Nuget
     connection: gh-rg
     name: OctocatApp
     version: 1.0.0
     
stages:
 - stage: ConsumeArtifact
   jobs:
     - job: job1
       pool: 
        name: Default
       steps:
        - getPackage: somePackage
